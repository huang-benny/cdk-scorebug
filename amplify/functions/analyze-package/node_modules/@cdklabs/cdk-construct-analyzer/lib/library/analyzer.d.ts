import type { ScoreResult, SignalWeights } from './types';
export declare class ConstructAnalyzer {
    private config;
    constructor();
    analyzePackage(packageName: string, weights?: SignalWeights): Promise<ScoreResult>;
    private calculateSignalScores;
    private convertLevelToPoints;
    private normalizePillarScores;
    private getTotalWeightForPillar;
    /**
     * Extract signal weights from config in the same structure as signalScores
     * Uses custom weights when provided, otherwise falls back to default weights
     */
    private getSignalWeights;
}
